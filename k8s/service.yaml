apiVersion: v1
kind: Service
metadata:
  name: devops-api-service
  labels:
    app: devops-api
    project: devops
spec:
  selector:
    app: devops-api
  ports:
  - name: http
    port: 80
    targetPort: 5000
    protocol: TCP
  type: NodePort  # Pour minikube/k3d/kind